---
# ~/.github/workflows/dependabot.yml
# Source: https://github.com/BetaHuhn/repo-file-sync-action/blob/master/.github/workflows/dependabot.yml
# $schema https://json.schemastore.org/github-workflow.json

name: Dependabot auto-merge

"on":
  # See https://stackoverflow.com/a/74959635
  pull_request_target:
  workflow_call: # Required so this workflow can be called from another workflow
  workflow_dispatch: # Allows you to run this workflow manually from the Actions tab

permissions:
  pull-requests: write
  contents: write

jobs:
  job:
    runs-on: ubuntu-latest
    # See https://github.com/dependabot/fetch-metadata?tab=readme-ov-file#enabling-auto-merge
    if: startsWith(github.event.pull_request.user.login, 'dependabot')
    steps:
      - name: Dependabot metadata
        id: dependabot-metadata
        uses: dependabot/fetch-metadata@v1.6.0
      - name: Enable auto-merge for Dependabot PRs
        # if: steps.dependabot-metadata.outputs.update-type == 'version-update:semver-patch'
        run: gh pr merge --auto --merge "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  
# name: auto-merge
#
# "on":
#   pull_request:
#
# permissions:
#   pull-requests: write
#   contents: write
#
# jobs:
#   job:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4.1.1
#       # See https://github.com/ahmadnassri/action-dependabot-auto-merge?tab=readme-ov-file#usage
#       - uses: ahmadnassri/action-dependabot-auto-merge@v2.6.6
#         with:
#           target: minor
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#
# cSpell:ignore ahmadnassri

# name: dependabot
#
# "on": # quotes required to quiet linter
#   schedule:
#     # run every day at 16:00 UTC (09:00 PDT)
#     - cron: "0 16 * * *"
#   workflow_call: # Required so this workflow can be called from another workflow
#   workflow_dispatch: # Allows you to run this workflow manually from the Actions tab
#
# permissions: write-all
#
# jobs:
#   dependabot:
#     name: Auto Merge
#     runs-on: ubuntu-latest
#     steps:
#       # Docs: https://github.com/AnandChowdhary/dependabot-pr-action
#       - name: USES AnandChowdhary/dependabot-pr-action@master
#         uses: AnandChowdhary/dependabot-pr-action@master
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           merge-minor: true
#           merge-patch: true
#
# cSpell:ignore Anand, Chowdhary
# eof
